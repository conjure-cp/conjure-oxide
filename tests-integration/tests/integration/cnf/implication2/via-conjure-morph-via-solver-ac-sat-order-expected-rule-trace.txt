Model before rewriting:

find a: bool
find b: bool
find c: bool

such that

(a) -> ((b) -> (c))

--

Final model:

find a: bool
find b: bool
find c: bool
find __0: bool
find __1: bool

such that

true

clauses:

(¬__0 \/ a),
(¬__0 \/ b),
(__0 \/ ¬a \/ ¬b),
(¬__1 \/ ¬__0 \/ c),
(__1 \/ __0),
(__1 \/ ¬c),
(__1)

