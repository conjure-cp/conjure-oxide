Model before rewriting:

find a: bool
find b: bool
find c: bool

such that

(a) -> ((b) -> (c))

--

(a) -> ((b) -> (c)), 
   ~~> normalise_implies_uncurry ([("Base", 8400)])
(and([a,b;int(1..)])) -> (c)

--

Final model:

find a: bool
find b: bool
find c: bool

such that

(and([a,b;int(1..)])) -> (c)

