Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

or([or([x,y;int(1..)]),z;int(1..)])

--

Final model:

find x: bool
find y: bool
find z: bool

such that

or([x,y,z;int(1..)])

Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

!(and([and([and([(true) -> (x),(false) -> (y);int(1..)]),(false) -> (z);int(1..)]),or([or([and([!(true),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]),and([!(false),z;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool
find z: bool

such that

!(and([and([and([(true) -> (x),(false) -> (y);int(1..)]),(false) -> (z);int(1..)]),or([or([and([!(true),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]),and([!(false),z;int(1..)]);int(1..)]);int(1..)]))

Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

!(and([and([and([(false) -> (x),(true) -> (y);int(1..)]),(false) -> (z);int(1..)]),or([or([and([!(false),x;int(1..)]),and([!(true),y;int(1..)]);int(1..)]),and([!(false),z;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool
find z: bool

such that

!(and([and([and([(false) -> (x),(true) -> (y);int(1..)]),(false) -> (z);int(1..)]),or([or([and([!(false),x;int(1..)]),and([!(true),y;int(1..)]);int(1..)]),and([!(false),z;int(1..)]);int(1..)]);int(1..)]))

Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

!(and([and([and([(false) -> (x),(false) -> (y);int(1..)]),(true) -> (z);int(1..)]),or([or([and([!(false),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]),and([!(true),z;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool
find z: bool

such that

!(and([and([and([(false) -> (x),(false) -> (y);int(1..)]),(true) -> (z);int(1..)]),or([or([and([!(false),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]),and([!(true),z;int(1..)]);int(1..)]);int(1..)]))

