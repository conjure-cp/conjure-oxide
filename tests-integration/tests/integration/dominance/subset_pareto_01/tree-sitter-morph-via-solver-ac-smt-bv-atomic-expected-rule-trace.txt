Model before rewriting:

find a: bool
find b: bool
find c: bool
find d: bool
find e: bool

such that

or([or([a,b;int(1..)]),c;int(1..)]),
or([b,d;int(1..)]),
or([c,e;int(1..)]),
!(and([a,d;int(1..)])),
!(and([b,e;int(1..)]))

--

Final model:

find a: bool
find b: bool
find c: bool
find d: bool
find e: bool

such that

or([a,b,c;int(1..)]),
or([b,d;int(1..)]),
or([c,e;int(1..)]),
or([!(a),!(d);int(1..)]),
or([!(b),!(e);int(1..)])

Model before rewriting:

find a: bool
find b: bool
find c: bool
find d: bool
find e: bool

such that

!(and([and([and([and([and([(false) -> (a),(true) -> (b);int(1..)]),(true) -> (c);int(1..)]),(false) -> (d);int(1..)]),(false) -> (e);int(1..)]),or([or([or([or([and([!(false),a;int(1..)]),and([!(true),b;int(1..)]);int(1..)]),and([!(true),c;int(1..)]);int(1..)]),and([!(false),d;int(1..)]);int(1..)]),and([!(false),e;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find a: bool
find b: bool
find c: bool
find d: bool
find e: bool

such that

!(and([and([and([and([and([(false) -> (a),(true) -> (b);int(1..)]),(true) -> (c);int(1..)]),(false) -> (d);int(1..)]),(false) -> (e);int(1..)]),or([or([or([or([and([!(false),a;int(1..)]),and([!(true),b;int(1..)]);int(1..)]),and([!(true),c;int(1..)]);int(1..)]),and([!(false),d;int(1..)]);int(1..)]),and([!(false),e;int(1..)]);int(1..)]);int(1..)]))

Model before rewriting:

find a: bool
find b: bool
find c: bool
find d: bool
find e: bool

such that

!(and([and([and([and([and([(false) -> (a),(false) -> (b);int(1..)]),(true) -> (c);int(1..)]),(true) -> (d);int(1..)]),(false) -> (e);int(1..)]),or([or([or([or([and([!(false),a;int(1..)]),and([!(false),b;int(1..)]);int(1..)]),and([!(true),c;int(1..)]);int(1..)]),and([!(true),d;int(1..)]);int(1..)]),and([!(false),e;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find a: bool
find b: bool
find c: bool
find d: bool
find e: bool

such that

!(and([and([and([and([and([(false) -> (a),(false) -> (b);int(1..)]),(true) -> (c);int(1..)]),(true) -> (d);int(1..)]),(false) -> (e);int(1..)]),or([or([or([or([and([!(false),a;int(1..)]),and([!(false),b;int(1..)]);int(1..)]),and([!(true),c;int(1..)]);int(1..)]),and([!(true),d;int(1..)]);int(1..)]),and([!(false),e;int(1..)]);int(1..)]);int(1..)]))

