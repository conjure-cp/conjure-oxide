Model before rewriting:

find x: bool
find y: bool

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find x: bool
find y: bool

such that

true

Model before rewriting:

find x: bool
find y: bool

such that

and([and([(x) -> (false),(y) -> (false);int(1..)]),or([and([!(x),false;int(1..)]),and([!(y),false;int(1..)]);int(1..)]);int(1..)])

--

Final model:

find x: bool
find y: bool

such that

and([and([(x) -> (false),(y) -> (false);int(1..)]),or([and([!(x),false;int(1..)]),and([!(y),false;int(1..)]);int(1..)]);int(1..)])

Model before rewriting:

find x: bool
find y: bool

such that

and([and([(x) -> (false),(y) -> (true);int(1..)]),or([and([!(x),false;int(1..)]),and([!(y),true;int(1..)]);int(1..)]);int(1..)])

--

Final model:

find x: bool
find y: bool

such that

and([and([(x) -> (false),(y) -> (true);int(1..)]),or([and([!(x),false;int(1..)]),and([!(y),true;int(1..)]);int(1..)]);int(1..)])

Model before rewriting:

find x: bool
find y: bool

such that

and([and([(x) -> (true),(y) -> (false);int(1..)]),or([and([!(x),true;int(1..)]),and([!(y),false;int(1..)]);int(1..)]);int(1..)])

--

Final model:

find x: bool
find y: bool

such that

and([and([(x) -> (true),(y) -> (false);int(1..)]),or([and([!(x),true;int(1..)]),and([!(y),false;int(1..)]);int(1..)]);int(1..)])

Model before rewriting:

find x: bool
find y: bool

such that

and([and([(x) -> (true),(y) -> (true);int(1..)]),or([and([!(x),true;int(1..)]),and([!(y),true;int(1..)]);int(1..)]);int(1..)])

--

Final model:

find x: bool
find y: bool

such that

and([and([(x) -> (true),(y) -> (true);int(1..)]),or([and([!(x),true;int(1..)]),and([!(y),true;int(1..)]);int(1..)]);int(1..)])

