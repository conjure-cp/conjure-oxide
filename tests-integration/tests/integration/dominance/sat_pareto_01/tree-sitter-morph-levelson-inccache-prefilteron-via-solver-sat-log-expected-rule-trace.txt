Model before rewriting:

find x: bool
find y: bool

--

Final model:

find x: bool
find y: bool

such that

true

Model before rewriting:

find x: bool
find y: bool

such that

!(and([and([(false) -> (x),(false) -> (y);int(1..)]),or([and([!(false),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool

such that

!(and([and([(false) -> (x),(false) -> (y);int(1..)]),or([and([!(false),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]);int(1..)]))

Model before rewriting:

find x: bool
find y: bool

such that

!(and([and([(true) -> (x),(false) -> (y);int(1..)]),or([and([!(true),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool

such that

!(and([and([(true) -> (x),(false) -> (y);int(1..)]),or([and([!(true),x;int(1..)]),and([!(false),y;int(1..)]);int(1..)]);int(1..)]))

Model before rewriting:

find x: bool
find y: bool

such that

!(and([and([(false) -> (x),(true) -> (y);int(1..)]),or([and([!(false),x;int(1..)]),and([!(true),y;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool

such that

!(and([and([(false) -> (x),(true) -> (y);int(1..)]),or([and([!(false),x;int(1..)]),and([!(true),y;int(1..)]);int(1..)]);int(1..)]))

Model before rewriting:

find x: bool
find y: bool

such that

!(and([and([(true) -> (x),(true) -> (y);int(1..)]),or([and([!(true),x;int(1..)]),and([!(true),y;int(1..)]);int(1..)]);int(1..)]))

--

Final model:

find x: bool
find y: bool

such that

!(and([and([(true) -> (x),(true) -> (y);int(1..)]),or([and([!(true),x;int(1..)]),and([!(true),y;int(1..)]);int(1..)]);int(1..)]))

