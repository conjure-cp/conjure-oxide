Model before rewriting:

find x: bool
find y: bool

such that

(product([toInt(x),toInt(y);int(1..2)]) = 1)

--

Final model:

find x: bool
find y: bool

such that

(product([toInt(x),toInt(y);int(1..)]) = 1)

