Model before rewriting:

find x: bool
find y: bool

such that

(product([toInt(x),toInt(y);int(1..2)]) = 1)

--

product([toInt(x),toInt(y);int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)]) 
product([toInt(x),toInt(y);int(1..)]) 

--

Final model:

find x: bool
find y: bool

such that

(product([toInt(x),toInt(y);int(1..)]) = 1)

