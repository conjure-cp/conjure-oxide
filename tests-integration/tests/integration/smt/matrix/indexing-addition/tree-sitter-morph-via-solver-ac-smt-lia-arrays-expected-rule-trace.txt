Model before rewriting:

find a: matrix indexed by [int(1..3)] of int(1..3)

such that

(sum([a[1],a[2];int(1..)]) = a[3])

--

Final model:

find a: matrix indexed by [int(1..3)] of int(1..3)

such that

(sum([a[1],a[2];int(1..)]) = a[3])

