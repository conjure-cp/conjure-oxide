Model before rewriting:

find m: matrix indexed by [int(0..1), int(0..1)] of int(0..1)
find m2: matrix indexed by [int(0..1), int(0..1)] of int(0..1)

such that

(m = m2)

--

(m = m2), 
   ~~> flatten_matrix_eq_neq ([("Smt", 3000)])
and([(m[0, 0] = m2[0, 0]),(m[0, 1] = m2[0, 1]),(m[1, 0] = m2[1, 0]),(m[1, 1] = m2[1, 1]);int(1..)])

--

and([(m[0, 0] = m2[0, 0]),(m[0, 1] = m2[0, 1]),(m[1, 0] = m2[1, 0]),(m[1, 1] = m2[1, 1]);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
(m[0, 0] = m2[0, 0]),
(m[0, 1] = m2[0, 1]),
(m[1, 0] = m2[1, 0]),
(m[1, 1] = m2[1, 1])

--

m[0, 0], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[0, 0])

--

m2[0, 0], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m2,[0, 0])

--

m[0, 1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[0, 1])

--

m2[0, 1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m2,[0, 1])

--

m[1, 0], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[1, 0])

--

m2[1, 0], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m2,[1, 0])

--

m[1, 1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[1, 1])

--

m2[1, 1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m2,[1, 1])

--

Final model:

find m: matrix indexed by [int(0..1), int(0..1)] of int(0..1)
find m2: matrix indexed by [int(0..1), int(0..1)] of int(0..1)

such that

(SafeIndex(m,[0, 0]) = SafeIndex(m2,[0, 0])),
(SafeIndex(m,[0, 1]) = SafeIndex(m2,[0, 1])),
(SafeIndex(m,[1, 0]) = SafeIndex(m2,[1, 0])),
(SafeIndex(m,[1, 1]) = SafeIndex(m2,[1, 1]))

