Model before rewriting:

find a: matrix indexed by [int(1..2, 4)] of bool

such that

and([a[1],a[2],a[4];int(1..3)])

--

a[1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[1])

--

a[2], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[2])

--

a[4], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[4])

--

and([SafeIndex(a,[1]),SafeIndex(a,[2]),SafeIndex(a,[4]);int(1..3)]), 
   ~~> matrix_to_list ([("Base", 2000)])
and([SafeIndex(a,[1]),SafeIndex(a,[2]),SafeIndex(a,[4]);int(1..)])

--

and([SafeIndex(a,[1]),SafeIndex(a,[2]),SafeIndex(a,[4]);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
SafeIndex(a,[1]),
SafeIndex(a,[2]),
SafeIndex(a,[4])

--

Final model:

find a: matrix indexed by [int(1..2, 4)] of bool

such that

SafeIndex(a,[1]),
SafeIndex(a,[2]),
SafeIndex(a,[4])

