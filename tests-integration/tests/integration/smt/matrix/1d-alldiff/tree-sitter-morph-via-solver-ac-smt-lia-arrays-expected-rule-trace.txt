Model before rewriting:

find a: matrix indexed by [int(1..3)] of int(1..3)

such that

allDiff(a),
allDiff(a[..]),
(a[1] <= a[2]),
(a[2] <= a[3])

--

Final model:

find a: matrix indexed by [int(1..3)] of int(1..3)

such that

allDiff([a[1],a[2],a[3];int(1..)]),
allDiff([a[1],a[2],a[3];int(1..)]),
(a[1] <= a[2]),
(a[2] <= a[3])

