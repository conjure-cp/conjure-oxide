Model before rewriting:

find a: matrix indexed by [[bool]] of bool

such that

(a[true] = !(a[false]))

--

a[true], 
   ~~> index_to_bubble ([("Bubble", 6000)]) 
{a[true] @ and([__inDomain(true,bool);int(1..)])} 

--

({a[true] @ and([__inDomain(true,bool);int(1..)])} = !(a[false])), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
(a[true] = !(a[false])) 

--

a[false], 
   ~~> index_to_bubble ([("Bubble", 6000)]) 
{a[false] @ and([__inDomain(false,bool);int(1..)])} 

--

(a[true] = !({a[false] @ and([__inDomain(false,bool);int(1..)])})), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
(a[true] = !(a[false])) 

--

Final model:

find a: matrix indexed by [[bool]] of bool

such that

(a[true] = !(a[false]))

