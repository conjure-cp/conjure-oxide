Model before rewriting:

find m: matrix indexed by [int(1..2)] of bool
find x: int(1..3)

such that

m[x]

--

Final model:

find m: matrix indexed by [int(1..2)] of bool
find x: int(1..3)

such that

m[x],
(x >= 1),
(x <= 2)

