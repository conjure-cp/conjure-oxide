Model before rewriting:

find x: bool
find y: bool

such that

(x) -> (y)

--

(x) -> (y), 
   ~~> apply_tseytin_imply ([("SAT", 8500)]) 
__0 
new variables:
  find __0: bool
new clauses:
  (¬__0 \/ ¬x \/ y)
  (__0 \/ x)
  (__0 \/ ¬y)
--

__0, 
   ~~> remove_single_atom ([("SAT", 8400)]) 
true 

new clauses:
  (__0)
--

Final model:

find x: bool
find y: bool
find __0: bool

such that

true

clauses:

(¬__0 \/ ¬x \/ y),
(__0 \/ x),
(__0 \/ ¬y),
(__0)

