Model before rewriting:

find x: bool
find y: bool

such that

and([x,y;int(1..2)])

--

and([x,y;int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)]) 
and([x,y;int(1..)]) 

--

and([x,y;int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
x,
y 

--

Final model:

find x: bool
find y: bool

such that

x,
y

