Model before rewriting:

find x: bool
find y: bool

such that

and([x,y;int(1..)])

--

and([x,y;int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
x,
y 

--

x,
y, 
   ~~> remove_single_atom ([("SAT", 8400)]) 
y 

new clauses:
  (x)
--

y, 
   ~~> remove_single_atom ([("SAT", 8400)]) 
true 

new clauses:
  (y)
--

Final model:

find x: bool
find y: bool

such that

true

clauses:

(x),
(y)

