find x: matrix indexed by [int(1..3), int(1..3)] of int(1..2)
such that
    x[1, ..] >=lex [1, 2, 2],
    forAll i: int(1..2) .
        $ sort rows ascending top to bottom
        x[i, ..] <lex x[i + 1, ..] /\
        $ sort columns descending left to right
        x[.., i] >lex x[.., i + 1]