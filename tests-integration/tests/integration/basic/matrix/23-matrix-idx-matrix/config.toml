parser = [
    #    "tree-sitter",
    "via-conjure",
]

rewriter = [
    "naive",
    # "morph",
]

comprehension-expander = [
    # "native",
    # "via-solver",
    "via-solver-ac",
]

solver = [
    "minion",
    # "sat-log",
    # "sat-direct",
    # "sat-order",
    # "smt-bv-arrays-nodiscrete",
    # "smt-bv-arrays",
    # "smt-bv-atomic-nodiscrete",
    # "smt-bv-atomic",
    # "smt-lia-arrays-nodiscrete",
    # "smt-lia-arrays",
    # "smt-lia-atomic-nodiscrete",
    # "smt-lia-atomic",
]

# Conjure flattens matrix-indexed-by-matrix into multiple integer index domains,
# while we preserve the matrix index structure. Flatten both for comparison.
flatten-matrices = true

