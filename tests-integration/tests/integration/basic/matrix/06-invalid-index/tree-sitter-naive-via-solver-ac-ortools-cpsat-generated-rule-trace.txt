Model before rewriting:

letting MATRIX be domain matrix indexed by [int(1..3), int(1..2)] of int(1..3)
find a: MATRIX

such that

allDiff(a[..,1]),
allDiff(a[..,2]),
allDiff(a[1,..]),
allDiff(a[2,..]),
allDiff(a[3,..]),
(a[1, 1] = 1),
(a[2, 3] = 1)

--

a[..,1], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a,[..,1])

--

a[..,2], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a,[..,2])

--

a[1,..], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a,[1,..])

--

a[2,..], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a,[2,..])

--

a[3,..], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a,[3,..])

--

a[1, 1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[1, 1])

--

a[2, 3], 
   ~~> index_to_bubble ([("Bubble", 6000)])
{SafeIndex(a,[2, 3]) @ false}

--

({SafeIndex(a,[2, 3]) @ false} = 1), 
   ~~> bubble_up ([("Bubble", 8800)])
{(SafeIndex(a,[2, 3]) = 1) @ false}

--

{(SafeIndex(a,[2, 3]) = 1) @ false}, 
   ~~> expand_bubble ([("Bubble", 8900)])
and([(SafeIndex(a,[2, 3]) = 1),false;int(1..)])

--

allDiff(SafeSlice(a,[..,1])),
allDiff(SafeSlice(a,[..,2])),
allDiff(SafeSlice(a,[1,..])),
allDiff(SafeSlice(a,[2,..])),
allDiff(SafeSlice(a,[3,..])),
(SafeIndex(a,[1, 1]) = 1),
and([(SafeIndex(a,[2, 3]) = 1),false;int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
false

--

Final model:

letting MATRIX be domain matrix indexed by [int(1..3), int(1..2)] of int(1..3)
find a: MATRIX

such that

false

