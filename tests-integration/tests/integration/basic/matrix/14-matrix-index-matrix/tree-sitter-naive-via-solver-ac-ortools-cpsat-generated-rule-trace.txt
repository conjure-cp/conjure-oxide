Model before rewriting:

find m: matrix indexed by [int(1..3)] of int(1..3)

such that

(m[m[1]] = 1),
(m[m[2]] = 2),
(m[m[3]] = 3)

--

m[m[1]], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[m[1]])

--

m[1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[1])

--

m[m[2]], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[m[2]])

--

m[2], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[2])

--

m[m[3]], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[m[3]])

--

m[3], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(m,[3])

--

Final model:

find m: matrix indexed by [int(1..3)] of int(1..3)

such that

(SafeIndex(m,[SafeIndex(m,[1])]) = 1),
(SafeIndex(m,[SafeIndex(m,[2])]) = 2),
(SafeIndex(m,[SafeIndex(m,[3])]) = 3)

