Model before rewriting:

letting MATRIX be domain matrix indexed by [int(1..3), int(1..2)] of int(1..3)
find a: MATRIX

such that

allDiff(a[..,1]),
allDiff(a[..,3]),
allDiff(a[1,..]),
allDiff(a[2,..]),
allDiff(a[3,..]),
(a[1, 1] = 1),
(a[2, 2] = 1)

--

a[..,1], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a,[..,1])

--

a[..,3], 
   ~~> slice_to_bubble ([("Bubble", 6000)])
{SafeSlice(a,[..,3]) @ false}

--

allDiff({SafeSlice(a,[..,3]) @ false}), 
   ~~> bubble_up ([("Bubble", 8800)])
{allDiff(SafeSlice(a,[..,3])) @ false}

--

{allDiff(SafeSlice(a,[..,3])) @ false}, 
   ~~> expand_bubble ([("Bubble", 8900)])
and([allDiff(SafeSlice(a,[..,3])),false;int(1..)])

--

allDiff(SafeSlice(a,[..,1])),
and([allDiff(SafeSlice(a,[..,3])),false;int(1..)]),
allDiff(a[1,..]),
allDiff(a[2,..]),
allDiff(a[3,..]),
(a[1, 1] = 1),
(a[2, 2] = 1), 
   ~~> constant_evaluator ([("Constant", 9001)])
false

--

Final model:

letting MATRIX be domain matrix indexed by [int(1..3), int(1..2)] of int(1..3)
find a: MATRIX

such that

false

