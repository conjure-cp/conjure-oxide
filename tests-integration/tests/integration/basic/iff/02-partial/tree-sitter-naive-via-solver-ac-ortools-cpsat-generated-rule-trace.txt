Model before rewriting:

find x: bool
find y: bool

such that

and([(x) <-> (x),(false) <-> (y);int(1..)])

--

and([(x) <-> (x),(false) <-> (y);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
!(y)

--

Final model:

find x: bool
find y: bool

such that

!(y)

