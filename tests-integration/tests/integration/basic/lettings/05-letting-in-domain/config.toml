parser = [
    "tree-sitter",
    "via-conjure",
]

rewriter = [
    "naive",
    "morph",
]

comprehension-expander = [
    # "native",
    # "via-solver",
    "via-solver-ac",
]

solver = [
    "minion",
    #"sat-log", (Ungrounded domains not supported by SAT yet)
    #"sat-direct", (Ungrounded domains not supported by SAT yet)
    #"sat-order", (Ungrounded domains not supported by SAT yet)
    # "smt-bv-arrays-nodiscrete",
    # "smt-bv-arrays",
    # "smt-bv-atomic-nodiscrete",
    # "smt-bv-atomic",
    # "smt-lia-arrays-nodiscrete",
    # "smt-lia-arrays",
    # "smt-lia-atomic-nodiscrete",
    # "smt-lia-atomic",
]
