Model before rewriting:

find a: bool
find b: bool

such that

and([a,(b = and([given k: int(1..2)

such that

(k <= 5)
 | k: int(1..2),]));int(1..2)])

--

(b = and([given k: int(1..2)

such that

(k <= 5)
 | k: int(1..2),])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)]) 
Reify(and([given k: int(1..2)

such that

(k <= 5)
 | k: int(1..2),]), b) 

--

Model before rewriting:

find k: int(1..2)

such that

(true != (k <= 5))

--

(true != (k <= 5)), 
   ~~> flatten_generic ([("Minion", 4200)]) 
(true != __0) 
new variables:
  find __0: bool
new constraints:
  __0 =aux (k <= 5)
--

__0 =aux (k <= 5), 
   ~~> bool_eq_to_reify ([("Minion", 4400)]) 
Reify((k <= 5), __0) 

--

(k <= 5), 
   ~~> leq_to_ineq ([("Minion", 4100)]) 
Ineq(k, 5, 0) 

--

Final model:

find k: int(1..2)
find __0: bool

such that

(true != __0),
Reify(Ineq(k, 5, 0), __0)

Model before rewriting:

given k: int(1..2)

such that

(k <= 5)

--

(k <= 5), 
   ~~> leq_to_ineq ([("Minion", 4100)]) 
Ineq(k, 5, 0) 

--

Final model:

given k: int(1..2)

such that

Ineq(k, 5, 0)

and([given k: int(1..2)

such that

(k <= 5)
 | k: int(1..2),__0: bool,]), 
   ~~> expand_comprehension_ac ([("Better_AC_Comprehension_Expansion", 2001)]) 
and([;int(1..)]) 

--

and([a,Reify(and([;int(1..)]), b);int(1..2)]), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
and([a,Reify(true, b);int(1..2)]) 

--

and([a,Reify(true, b);int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)]) 
and([a,Reify(true, b);int(1..)]) 

--

and([a,Reify(true, b);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
a,
Reify(true, b) 

--

Final model:

find a: bool
find b: bool

such that

a,
Reify(true, b)

