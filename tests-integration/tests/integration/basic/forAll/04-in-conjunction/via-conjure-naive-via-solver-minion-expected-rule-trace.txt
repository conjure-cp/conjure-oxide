Model before rewriting:

find a: bool
find b: bool

such that

and([a,(b = and([ (k <= 5) | k : int(1..2) ]));int(1..2)])

--

(b = and([ (k <= 5) | k : int(1..2) ])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(and([ (k <= 5) | k : int(1..2) ]), b)

--

and([a,Reify(and([ (k <= 5) | k : int(1..2) ]), b);int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)])
and([a,Reify(and([ (k <= 5) | k : int(1..2) ]), b);int(1..)])

--

and([a,Reify(and([ (k <= 5) | k : int(1..2) ]), b);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
a,
Reify(and([ (k <= 5) | k : int(1..2) ]), b)

--

Via-solver generator model before rewriting:

find k: int(1..2)

--

Model before rewriting:

find k: int(1..2)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find k: int(1..2)

such that

true

[ (k <= 5) | k : int(1..2) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[true,true;int(1..)]

--

a,
Reify(and([true,true;int(1..)]), b), 
   ~~> constant_evaluator ([("Constant", 9001)])
a,
Reify(true, b)

--

Final model:

find a: bool
find b: bool

such that

a,
Reify(true, b)

