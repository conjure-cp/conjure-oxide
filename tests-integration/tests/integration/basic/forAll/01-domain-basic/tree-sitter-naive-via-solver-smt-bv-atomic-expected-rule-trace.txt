Model before rewriting:

find x: bool

such that

(x = and([ (i > 0) | i : int(1..5) ]))

--

Via-solver generator model before rewriting:

find i: int(1..5)

--

Model before rewriting:

find i: int(1..5)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find i: int(1..5)

such that

true

[ (i > 0) | i : int(1..5) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[true,true,true,true,true;int(1..)]

--

(x = and([true,true,true,true,true;int(1..)])), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x = true)

--

Final model:

find x: bool

such that

(x = true)

