# TODO: enable this test when the native parser supports literal collections in quantifier expressions

language ESSENCE' 1.0
find result: bool

such that result = (forAll i : int(1..3) . i > 0) /\ (exists j in [1,2,3] . j = 2)
