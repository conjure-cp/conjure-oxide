Model before rewriting:

find result: bool

such that

(result = and([ and([(n > 0),(n < 10);int(1..)]) | n : int(1..3) ]))

--

(result = and([ and([(n > 0),(n < 10);int(1..)]) | n : int(1..3) ])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(and([ and([(n > 0),(n < 10);int(1..)]) | n : int(1..3) ]), result)

--

Via-solver generator model before rewriting:

find n: int(1..3)

--

Model before rewriting:

find n: int(1..3)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find n: int(1..3)

such that

true

[ and([(n > 0),(n < 10);int(1..)]) | n : int(1..3) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[true,true,true;int(1..)]

--

Reify(and([true,true,true;int(1..)]), result), 
   ~~> constant_evaluator ([("Constant", 9001)])
Reify(true, result)

--

Final model:

find result: bool

such that

Reify(true, result)

