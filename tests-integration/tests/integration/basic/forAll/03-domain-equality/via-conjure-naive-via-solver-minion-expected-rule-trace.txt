Model before rewriting:

find y: bool

such that

(y = and([ (product([j,j;int(1..2)]) >= j) | j : int(0..3) ]))

--

(y = and([ (product([j,j;int(1..2)]) >= j) | j : int(0..3) ])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(and([ (product([j,j;int(1..2)]) >= j) | j : int(0..3) ]), y)

--

Via-solver generator model before rewriting:

find j: int(0..3)

--

Model before rewriting:

find j: int(0..3)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find j: int(0..3)

such that

true

[ (product([j,j;int(1..2)]) >= j) | j : int(0..3) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[true,true,true,true;int(1..)]

--

Reify(and([true,true,true,true;int(1..)]), y), 
   ~~> constant_evaluator ([("Constant", 9001)])
Reify(true, y)

--

Final model:

find y: bool

such that

Reify(true, y)

