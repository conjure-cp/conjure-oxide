Model before rewriting:

find x: bool

such that

(x = or([ (product([i,i;int(1..)]) = i) | i : int(0..5) ]))

--

(x = or([ (product([i,i;int(1..)]) = i) | i : int(0..5) ])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)]) 
Reify(or([ (product([i,i;int(1..)]) = i) | i : int(0..5) ]), x) 

--

[ (product([i,i;int(1..)]) = i) | i : int(0..5) ], 
   ~~> expand_comprehension ([("Base", 2000)]) 
[true,true,false,false,false,false;int(1..)] 

--

Reify(or([true,true,false,false,false,false;int(1..)]), x), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
Reify(true, x) 

--

Final model:

find x: bool

such that

Reify(true, x)

