Model before rewriting:

find x: bool

such that

(x = or([ (product([i,i;int(1..)]) = i) | i : int(0..5) ]))

--

Via-solver generator model before rewriting:

find i: int(0..5)

--

Model before rewriting:

find i: int(0..5)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find i: int(0..5)

such that

true

[ (product([i,i;int(1..)]) = i) | i : int(0..5) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[true,true,false,false,false,false;int(1..)]

--

(x = or([true,true,false,false,false,false;int(1..)])), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x = true)

--

(x = true), 
   ~~> apply_tseytin_iff_eq ([("SAT", 8500)])
__0
new variables:
  find __0: bool
new clauses:
  (¬x \/ __0)
  (x \/ ¬__0)

--

__0, 
   ~~> remove_single_atom ([("SAT", 8400)])
true
new clauses:
  (__0)

--

Final model:

find x: bool
find __0: bool

such that

true

clauses:

(¬x \/ __0),
(x \/ ¬__0),
(__0)

