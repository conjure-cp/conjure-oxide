Model before rewriting:

find x: bool

such that

(x = or([ (product([i,i;int(1..2)]) = i) | i : int(0..5) ]))

--

Model before rewriting:

find i: int(0..5)

such that

(false != (product([i,i;int(1..2)]) = i))

--

(false != (product([i,i;int(1..2)]) = i)), 
   ~~> flatten_generic ([("Minion", 4200)])
(false != __0)
new variables:
  find __0: bool
new constraints:
  __0 =aux (product([i,i;int(1..2)]) = i)

--

__0 =aux (product([i,i;int(1..2)]) = i), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify((product([i,i;int(1..2)]) = i), __0)

--

product([i,i;int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)])
product([i,i;int(1..)])

--

(product([i,i;int(1..)]) = i), 
   ~~> introduce_producteq ([("Minion", 4200)])
FlatProductEq(i,i,i)

--

Final model:

find i: int(0..5)
find __0: bool

such that

(false != __0),
Reify(FlatProductEq(i,i,i), __0)

or([ (product([i,i;int(1..2)]) = i) | i : int(0..5) ]), 
   ~~> expand_comprehension_via_solver_ac ([("Base", 2002)])
or([true,true;int(1..)])

--

(x = or([true,true;int(1..)])), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x = true)

--

Final model:

find x: bool

such that

(x = true)

