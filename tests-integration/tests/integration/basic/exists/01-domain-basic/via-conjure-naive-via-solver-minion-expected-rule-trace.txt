Model before rewriting:

find x: bool

such that

(x = or([ (product([i,i;int(1..2)]) = i) | i : int(0..5) ]))

--

(x = or([ (product([i,i;int(1..2)]) = i) | i : int(0..5) ])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(or([ (product([i,i;int(1..2)]) = i) | i : int(0..5) ]), x)

--

Via-solver generator model before rewriting:

find i: int(0..5)

--

Model before rewriting:

find i: int(0..5)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find i: int(0..5)

such that

true

[ (product([i,i;int(1..2)]) = i) | i : int(0..5) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[true,true,false,false,false,false;int(1..)]

--

Reify(or([true,true,false,false,false,false;int(1..)]), x), 
   ~~> constant_evaluator ([("Constant", 9001)])
Reify(true, x)

--

Final model:

find x: bool

such that

Reify(true, x)

