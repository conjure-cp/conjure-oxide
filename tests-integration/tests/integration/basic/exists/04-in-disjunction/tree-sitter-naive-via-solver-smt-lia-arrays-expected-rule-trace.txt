Model before rewriting:

find a: bool
find b: bool

such that

or([a,(b = or([ (product([m,m;int(1..)]) = 4) | m : int(0..3) ]));int(1..)])

--

Via-solver generator model before rewriting:

find m: int(0..3)

--

Model before rewriting:

find m: int(0..3)

--

, 
   ~~> eval_root ([("Constant", 9001)])
true

--

Final model:

find m: int(0..3)

such that

true

[ (product([m,m;int(1..)]) = 4) | m : int(0..3) ], 
   ~~> expand_comprehension_via_solver ([("Base", 2000)])
[false,false,true,false;int(1..)]

--

or([a,(b = or([false,false,true,false;int(1..)]));int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
or([a,(b = true);int(1..)])

--

Final model:

find a: bool
find b: bool

such that

or([a,(b = true);int(1..)])

