Model before rewriting:

find check: bool

such that

(check = or([ and([(p > 2),(p < 4);int(1..2)]) | p : int(1..5) ]))

--

[ and([(p > 2),(p < 4);int(1..2)]) | p : int(1..5) ], 
   ~~> expand_comprehension_native ([("Base", 2000)])
[false,false,true,false,false;int(1..)]

--

(check = or([false,false,true,false,false;int(1..)])), 
   ~~> constant_evaluator ([("Constant", 9001)])
(check = true)

--

(check = true), 
   ~~> apply_tseytin_iff_eq ([("SAT", 8500)])
__0
new variables:
  find __0: bool
new clauses:
  (¬check \/ __0)
  (check \/ ¬__0)

--

__0, 
   ~~> remove_single_atom ([("SAT", 8400)])
true
new clauses:
  (__0)

--

Final model:

find check: bool
find __0: bool

such that

true

clauses:

(¬check \/ __0),
(check \/ ¬__0),
(__0)

