language ESSENCE' 1.0

find x: bool

such that

$ we cannot do any optimisations or pruning for this comprehension as it is not inside an ac operator.
$
$ Test that expansion and static guards still work without these optimisations.

x = allDiff([x | x: int(1..10), x %2 = 0])
