Model before rewriting:

find x: bool

such that

(x = allDiff([ x | x : int(1..10), (x % 2 = 0) ]))

--

(x = allDiff([ x | x : int(1..10), (x % 2 = 0) ])), 
   ~~> bool_eq_to_reify ([("Minion", 4400)]) 
Reify(allDiff([ x | x : int(1..10), (x % 2 = 0) ]), x) 

--

[ x | x : int(1..10), (x % 2 = 0) ], 
   ~~> expand_comprehension ([("Base", 2000)]) 
[2,4,6,8,10;int(1..)] 

--

Reify(allDiff([2,4,6,8,10;int(1..)]), x), 
   ~~> constant_evaluator ([("Constant", 9001)]) 
Reify(true, x) 

--

Final model:

find x: bool

such that

Reify(true, x)

