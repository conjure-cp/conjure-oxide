Model before rewriting:

find x: bool

such that

(x = allDiff([ x | x : int(1..10), (x % 2 = 0) ]))

--

(x = allDiff([ x | x : int(1..10), (x % 2 = 0) ])), 
   ~~> eagerly_expand_native_comprehensions_in_small_roots ([("Base", 9500)])
(x = allDiff([2,4,6,8,10;int(1..)]))

--

(x = allDiff([2,4,6,8,10;int(1..)])), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x = true)

--

(x = true), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(true, x)

--

Final model:

find x: bool

such that

Reify(true, x)

