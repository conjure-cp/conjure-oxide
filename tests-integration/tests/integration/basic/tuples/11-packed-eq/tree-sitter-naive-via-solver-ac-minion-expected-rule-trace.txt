Model before rewriting:

find x: tuple of (int(1..3), int(1..4))
find y: tuple of (int(1..3), int(1..4))

such that

(x = y)

--

(x = y), 
   ~~> select_packed_for_comparison ([("ReprTuplePacked", 9500)])
(x = y)
new variables:
  find x#TuplePacked_1: int(0..11)
  find y#TuplePacked_1: int(0..11)

--

(x = y), 
   ~~> tuple_packed_var_eq_var ([("ReprTuplePacked", 2000)])
(x#TuplePacked_1 = y#TuplePacked_1)

--

Final model:

find x: tuple of (int(1..3), int(1..4))
find y: tuple of (int(1..3), int(1..4))
find x#TuplePacked_1: int(0..11)
find y#TuplePacked_1: int(0..11)

such that

(x#TuplePacked_1 = y#TuplePacked_1)

