Model before rewriting:

letting v be domain tuple of (int(1..2), int(3..4))
find x: v

such that

(x = (1,3))

--

(x = (1,3)), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x = (1,3))

--

x, 
   ~~> select_representation ([("ReprGeneral", 8000)])
x
new variables:
  find x#TupleToAtom_1: int(1..2)
  find x#TupleToAtom_2: int(3..4)

--

(x = (1,3)), 
   ~~> tuple_var_eq_lit ([("ReprGeneral", 2000)])
and([(x#TupleToAtom_1 = 1),(x#TupleToAtom_2 = 3);int(1..)])

--

and([(x#TupleToAtom_1 = 1),(x#TupleToAtom_2 = 3);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x#TupleToAtom_1 = 1),
(x#TupleToAtom_2 = 3)

--

Final model:

letting v be domain tuple of (int(1..2), int(3..4))
find x: tuple of (int(1..2), int(3..4))
find x#TupleToAtom_1: int(1..2)
find x#TupleToAtom_2: int(3..4)

such that

(x#TupleToAtom_1 = 1),
(x#TupleToAtom_2 = 3)

