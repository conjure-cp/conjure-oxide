Both parser's fail.

Tree-sitter parser doesn't have support for enum yet and conjure gives internal parser error.

sterr.expected from Conjure:
Error:
    In a letting statement: letting s be domain int(S)
    Error:
        Expected an integer, but got: set of direction
        In domain: int(S)
