find x : int(0..20)
find y : int(0..20)

such that
    x + y >= 20

dominance relation
    (x <= fromSolution(x)) /\
    (y <= fromSolution(y)) /\
    (
        (x < fromSolution(x)) \/
        (y < fromSolution(y))
    )
