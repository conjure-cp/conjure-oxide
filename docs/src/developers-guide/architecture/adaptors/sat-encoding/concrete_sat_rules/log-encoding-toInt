# Log Encoding Negation

## Overview

```text
toInt(bool) ~> SATInt(a)
```

## Rule Method
- Create a SATInt with a range [0,1]
- Assign the value of the 1's bit to be the boolean expression inside `toInt`
