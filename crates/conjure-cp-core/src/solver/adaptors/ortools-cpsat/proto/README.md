# OR-Tools CP-SAT protobuf source

This directory contains upstream protobuf definitions used by the Conjure Oxide
OR-Tools CP-SAT backend.

- Upstream path: `https://github.com/google/or-tools/blob/stable/ortools/sat/cp_model.proto`
- Local file: `cp_model.proto`
- Imported on: `2026-04-27`

## Update

1. Replace `cp_model.proto` with the upstream file
2. Re-run build so `prost-build` regenerates Rust types
3. Run project checks/tests to confirm compatibility
