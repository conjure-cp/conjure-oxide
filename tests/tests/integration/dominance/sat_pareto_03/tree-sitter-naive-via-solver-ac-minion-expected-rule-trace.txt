Model before rewriting:

find s1: bool
find s2: bool
find s3: bool
find s4: bool
find s5: bool
find s6: bool

such that

or([s1,s2;int(1..)]),
or([s2,s3;int(1..)]),
or([s3,s4;int(1..)]),
or([s4,s5;int(1..)]),
or([s5,s6;int(1..)]),
!(and([s1,s4;int(1..)])),
!(and([s2,s5;int(1..)])),
!(and([s3,s6;int(1..)]))

--

!(and([s1,s4;int(1..)])), 
   ~~> distribute_not_over_and ([("Base", 8400)])
or([!(s1),!(s4);int(1..)])

--

!(and([s2,s5;int(1..)])), 
   ~~> distribute_not_over_and ([("Base", 8400)])
or([!(s2),!(s5);int(1..)])

--

!(and([s3,s6;int(1..)])), 
   ~~> distribute_not_over_and ([("Base", 8400)])
or([!(s3),!(s6);int(1..)])

--

!(s1), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(s1,false)

--

!(s4), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(s4,false)

--

!(s2), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(s2,false)

--

!(s5), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(s5,false)

--

!(s3), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(s3,false)

--

!(s6), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(s6,false)

--

Final model:

find s1: bool
find s2: bool
find s3: bool
find s4: bool
find s5: bool
find s6: bool

such that

or([s1,s2;int(1..)]),
or([s2,s3;int(1..)]),
or([s3,s4;int(1..)]),
or([s4,s5;int(1..)]),
or([s5,s6;int(1..)]),
or([WatchedLiteral(s1,false),WatchedLiteral(s4,false);int(1..)]),
or([WatchedLiteral(s2,false),WatchedLiteral(s5,false);int(1..)]),
or([WatchedLiteral(s3,false),WatchedLiteral(s6,false);int(1..)])

