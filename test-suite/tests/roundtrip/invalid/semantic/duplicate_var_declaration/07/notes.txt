Both fail.

Tree-sitter parser doesn't support setting a size for a set and mset. 
Conjure parser throws an internal parser error: failure to create a symbol table because of the duplicate. 

Expected: 

Error:
    In a letting statement: letting s be domain int(S)
    Error:
        Expected an integer, but got: set of direction
        In domain: int(S)
