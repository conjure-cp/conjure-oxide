Model before rewriting:

find x: int(1..10)
find y: int(1..10)

such that

sum([x,y;int(1..)]) = 9,
sum([x,x;int(1..)]) = y

--

sum([x,y;int(1..)]) = 9
   ~~> introduce_weighted_sumleq_sumgeq ([("Minion", 4600)])
and([SumLeq([x, y], 9),SumGeq([x, y], 9);int(1..)])

--

and([SumLeq([x, y], 9),SumGeq([x, y], 9);int(1..)]),
sum([x,x;int(1..)]) = y
   ~~> constant_evaluator ([("Constant", 9001)])
SumLeq([x, y], 9),
SumGeq([x, y], 9),
sum([x,x;int(1..)]) = y

--

sum([x,x;int(1..)]) = y
   ~~> introduce_weighted_sumleq_sumgeq ([("Minion", 4600)])
and([FlatWeightedSumLeq([2],[x],y),FlatWeightedSumGeq([2],[x],y);int(1..)])

--

SumLeq([x, y], 9),
SumGeq([x, y], 9),
and([FlatWeightedSumLeq([2],[x],y),FlatWeightedSumGeq([2],[x],y);int(1..)])
   ~~> constant_evaluator ([("Constant", 9001)])
SumLeq([x, y], 9),
SumGeq([x, y], 9),
FlatWeightedSumLeq([2],[x],y),
FlatWeightedSumGeq([2],[x],y)

--

Final model:

find x: int(1..10)
find y: int(1..10)

such that

SumLeq([x, y], 9),
SumGeq([x, y], 9),
FlatWeightedSumLeq([2],[x],y),
FlatWeightedSumGeq([2],[x],y)

