Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

!(and([y,z;int(1..)]))

--

!(and([y,z;int(1..)])), 
   ~~> distribute_not_over_and ([("Base", 8400)])
or([!(y),!(z);int(1..)])

--

!(y), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(y,false)

--

!(z), 
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(z,false)

--

Final model:

find x: bool
find y: bool
find z: bool

such that

or([WatchedLiteral(y,false),WatchedLiteral(z,false);int(1..)])

