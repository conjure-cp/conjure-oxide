Model before rewriting:

find a: matrix indexed by [int(1..3)] of int(1..3)

such that

sum([a[1],a[2];int(1..)]) = a[3]

--

sum([a[1],a[2];int(1..)]) = a[3]
   ~~> select_repr_mta ([("ReprMatrixToAtom", 8500)])
sum([a[1],a[2];int(1..)]) = a[3]
new variables:
  find a#MatrixToAtom_1: int(1..3)
  find a#MatrixToAtom_2: int(1..3)
  find a#MatrixToAtom_3: int(1..3)

--

a[3]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[3])

--

a[1]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[1])

--

a[2]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[2])

--

SafeIndex(a,[3])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_3

--

SafeIndex(a,[1])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_1

--

SafeIndex(a,[2])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_2

--

sum([a#MatrixToAtom_1,a#MatrixToAtom_2;int(1..)]) = a#MatrixToAtom_3
   ~~> introduce_weighted_sumleq_sumgeq ([("Minion", 4600)])
and([SumLeq([a#MatrixToAtom_1, a#MatrixToAtom_2], a#MatrixToAtom_3),SumGeq([a#MatrixToAtom_1, a#MatrixToAtom_2], a#MatrixToAtom_3);int(1..)])

--

Final model:

find a: matrix indexed by [int(1..3)] of int(1..3)
find a#MatrixToAtom_1: int(1..3)
find a#MatrixToAtom_2: int(1..3)
find a#MatrixToAtom_3: int(1..3)

such that

and([SumLeq([a#MatrixToAtom_1, a#MatrixToAtom_2], a#MatrixToAtom_3),SumGeq([a#MatrixToAtom_1, a#MatrixToAtom_2], a#MatrixToAtom_3);int(1..)])

