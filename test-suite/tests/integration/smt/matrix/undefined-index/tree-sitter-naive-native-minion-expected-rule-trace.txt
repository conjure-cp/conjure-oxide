Model before rewriting:

find m: matrix indexed by [int(1..3)] of int(0..1)

such that

!((m[4] = 0))

--

!((m#matrix_to_atom[4] = 0)), 
   ~~> select_representation_matrix ([("Representations", 8001)])
!((m#matrix_to_atom[4] = 0))
new variables:
  find m#matrix_to_atom_1: int(0..1)
  find m#matrix_to_atom_2: int(0..1)
  find m#matrix_to_atom_3: int(0..1)

--

m#matrix_to_atom[4], 
   ~~> index_to_bubble ([("Bubble", 6000)])
{SafeIndex(m#matrix_to_atom,[4]) @ false}

--

({SafeIndex(m#matrix_to_atom,[4]) @ false} = 0), 
   ~~> bubble_up ([("Bubble", 8800)])
{(SafeIndex(m#matrix_to_atom,[4]) = 0) @ false}

--

{(SafeIndex(m#matrix_to_atom,[4]) = 0) @ false}, 
   ~~> expand_bubble ([("Bubble", 8900)])
and([(SafeIndex(m#matrix_to_atom,[4]) = 0),false;int(1..)])

--

!(and([(SafeIndex(m#matrix_to_atom,[4]) = 0),false;int(1..)])), 
   ~~> constant_evaluator ([("Constant", 9001)])
true

--

Final model:

find m: matrix indexed by [int(1..3)] of int(0..1)
find m#matrix_to_atom_1: int(0..1)
find m#matrix_to_atom_2: int(0..1)
find m#matrix_to_atom_3: int(0..1)

such that

true

