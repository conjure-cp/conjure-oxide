Model before rewriting:

find a: matrix indexed by [int(1..2, 4)] of bool

such that

and([a[1],a[2],a[4];int(1..3)])

--

and([a[1],a[2],a[4];int(1..3)])
   ~~> select_repr_mta ([("ReprMatrixToAtom", 8500)])
and([a[1],a[2],a[4];int(1..3)])
new variables:
  find a#MatrixToAtom_1: bool
  find a#MatrixToAtom_2: bool
  find a#MatrixToAtom_3: bool

--

a[1]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[1])

--

a[2]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[2])

--

a[4]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[4])

--

SafeIndex(a,[1])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_1

--

SafeIndex(a,[2])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_2

--

SafeIndex(a,[4])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_3

--

and([a#MatrixToAtom_1,a#MatrixToAtom_2,a#MatrixToAtom_3;int(1..3)])
   ~~> matrix_to_list ([("Base", 2000)])
and([a#MatrixToAtom_1,a#MatrixToAtom_2,a#MatrixToAtom_3;int(1..)])

--

Final model:

find a: matrix indexed by [int(1..2, 4)] of bool
find a#MatrixToAtom_1: bool
find a#MatrixToAtom_2: bool
find a#MatrixToAtom_3: bool

such that

and([a#MatrixToAtom_1,a#MatrixToAtom_2,a#MatrixToAtom_3;int(1..)])

