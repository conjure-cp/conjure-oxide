Model before rewriting:

find a: matrix indexed by [int(1..2, 4)] of bool

such that

and([a[1],a[2],a[4];int(1..3)])

--

and([a#matrix_to_atom[1],a#matrix_to_atom[2],a#matrix_to_atom[4];int(1..3)]), 
   ~~> select_representation_matrix ([("Representations", 8001)])
and([a#matrix_to_atom[1],a#matrix_to_atom[2],a#matrix_to_atom[4];int(1..3)])
new variables:
  find a#matrix_to_atom_1: bool
  find a#matrix_to_atom_2: bool
  find a#matrix_to_atom_4: bool

--

a#matrix_to_atom[1], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a#matrix_to_atom,[1])

--

a#matrix_to_atom[2], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a#matrix_to_atom,[2])

--

a#matrix_to_atom[4], 
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a#matrix_to_atom,[4])

--

and([SafeIndex(a#matrix_to_atom,[1]),SafeIndex(a#matrix_to_atom,[2]),SafeIndex(a#matrix_to_atom,[4]);int(1..3)]), 
   ~~> index_matrix_to_atom ([("Base", 5000)])
and([a#matrix_to_atom_1,a#matrix_to_atom_2,a#matrix_to_atom_4;int(1..3)])

--

and([a#matrix_to_atom_1,a#matrix_to_atom_2,a#matrix_to_atom_4;int(1..3)]), 
   ~~> matrix_to_list ([("Base", 2000)])
and([a#matrix_to_atom_1,a#matrix_to_atom_2,a#matrix_to_atom_4;int(1..)])

--

and([a#matrix_to_atom_1,a#matrix_to_atom_2,a#matrix_to_atom_4;int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
a#matrix_to_atom_1,
a#matrix_to_atom_2,
a#matrix_to_atom_4

--

Final model:

find a: matrix indexed by [int(1..2, 4)] of bool
find a#matrix_to_atom_1: bool
find a#matrix_to_atom_2: bool
find a#matrix_to_atom_4: bool

such that

a#matrix_to_atom_1,
a#matrix_to_atom_2,
a#matrix_to_atom_4

