Model before rewriting:

find a: matrix indexed by [bool] of bool

such that

(a[true] = !(a[false]))

--

Final model:

find a: matrix indexed by [bool] of bool

such that

(SafeIndex(a,[true]) = !(SafeIndex(a,[false])))

