Model before rewriting:

find a: matrix indexed by [bool] of bool

such that

a[true] = !(a[false])

--

a[true] = !(a[false])
   ~~> select_repr_mta ([("ReprMatrixToAtom", 8500)])
a[true] = !(a[false])
new variables:
  find a#MatrixToAtom_1: bool
  find a#MatrixToAtom_2: bool

--

a[true]
   ~~> unsafe_const_index_matrix_to_atom ([("ReprMatrixToAtom", 6500)])
a#MatrixToAtom_2

--

a[false]
   ~~> unsafe_const_index_matrix_to_atom ([("ReprMatrixToAtom", 6500)])
a#MatrixToAtom_1

--

a#MatrixToAtom_2 = !(a#MatrixToAtom_1)
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(!(a#MatrixToAtom_1), a#MatrixToAtom_2)

--

!(a#MatrixToAtom_1)
   ~~> not_literal_to_wliteral ([("Minion", 4100)])
WatchedLiteral(a#MatrixToAtom_1,false)

--

Final model:

find a: matrix indexed by [bool] of bool
find a#MatrixToAtom_1: bool
find a#MatrixToAtom_2: bool

such that

Reify(WatchedLiteral(a#MatrixToAtom_1,false), a#MatrixToAtom_2)

