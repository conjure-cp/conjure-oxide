Model before rewriting:

find a: matrix indexed by [int(1..2), int(1..2)] of int(1..4)
find b: matrix indexed by [int(1..4)] of int(1..4)

such that

allDifferent(flatten(a)),
flatten(a)[1] = b[1],
flatten(a)[2] = b[2],
flatten(a)[3] = b[3],
flatten(a)[4] = b[4]

--

allDifferent(flatten(a)),
flatten(a)[1] = b[1],
flatten(a)[2] = b[2],
flatten(a)[3] = b[3],
flatten(a)[4] = b[4]
   ~~> select_repr_mta ([("ReprMatrixToAtom", 8500)])
allDifferent(flatten(a)),
flatten(a)[1] = b[1],
flatten(a)[2] = b[2],
flatten(a)[3] = b[3],
flatten(a)[4] = b[4]
new variables:
  find a#MatrixToAtom_1: int(1..4)
  find a#MatrixToAtom_2: int(1..4)
  find a#MatrixToAtom_3: int(1..4)
  find a#MatrixToAtom_4: int(1..4)
  find b#MatrixToAtom_1: int(1..4)
  find b#MatrixToAtom_2: int(1..4)
  find b#MatrixToAtom_3: int(1..4)
  find b#MatrixToAtom_4: int(1..4)

--

flatten(a)[1]
   ~~> indexed_flatten_matrix ([("Base", 8001)])
a[1, 1]

--

flatten(a)[2]
   ~~> indexed_flatten_matrix ([("Base", 8001)])
a[1, 2]

--

flatten(a)[3]
   ~~> indexed_flatten_matrix ([("Base", 8001)])
a[2, 1]

--

flatten(a)[4]
   ~~> indexed_flatten_matrix ([("Base", 8001)])
a[2, 2]

--

a[1, 1]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[1, 1])

--

a[1, 2]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[1, 2])

--

a[2, 1]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[2, 1])

--

a[2, 2]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a,[2, 2])

--

b[1]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(b,[1])

--

b[2]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(b,[2])

--

b[3]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(b,[3])

--

b[4]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(b,[4])

--

SafeIndex(a,[1, 1])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_1

--

SafeIndex(a,[1, 2])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_2

--

SafeIndex(a,[2, 1])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_3

--

SafeIndex(a,[2, 2])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
a#MatrixToAtom_4

--

SafeIndex(b,[1])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
b#MatrixToAtom_1

--

SafeIndex(b,[2])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
b#MatrixToAtom_2

--

SafeIndex(b,[3])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
b#MatrixToAtom_3

--

SafeIndex(b,[4])
   ~~> index_matrix_to_atom ([("ReprMatrixToAtom", 5000)])
b#MatrixToAtom_4

--

flatten(a)
   ~~> matrix_flatten_to_atom ([("ReprMatrixToAtom", 5000)])
[a#MatrixToAtom_1,a#MatrixToAtom_2,a#MatrixToAtom_3,a#MatrixToAtom_4;int(1..)]

--

allDifferent([a#MatrixToAtom_1,a#MatrixToAtom_2,a#MatrixToAtom_3,a#MatrixToAtom_4;int(1..)])
   ~~> introduce_flat_alldiff ([("Minion", 4200)])
__flat_alldiff([a#MatrixToAtom_1, a#MatrixToAtom_2, a#MatrixToAtom_3, a#MatrixToAtom_4])

--

Final model:

find a: matrix indexed by [int(1..2), int(1..2)] of int(1..4)
find b: matrix indexed by [int(1..4)] of int(1..4)
find a#MatrixToAtom_1: int(1..4)
find a#MatrixToAtom_2: int(1..4)
find a#MatrixToAtom_3: int(1..4)
find a#MatrixToAtom_4: int(1..4)
find b#MatrixToAtom_1: int(1..4)
find b#MatrixToAtom_2: int(1..4)
find b#MatrixToAtom_3: int(1..4)
find b#MatrixToAtom_4: int(1..4)

such that

__flat_alldiff([a#MatrixToAtom_1, a#MatrixToAtom_2, a#MatrixToAtom_3, a#MatrixToAtom_4]),
a#MatrixToAtom_1 = b#MatrixToAtom_1,
a#MatrixToAtom_2 = b#MatrixToAtom_2,
a#MatrixToAtom_3 = b#MatrixToAtom_3,
a#MatrixToAtom_4 = b#MatrixToAtom_4

