Model before rewriting:

find m: matrix indexed by [int(0..1), int(0..1)] of int(0..1)

such that

(sum([m[0, 0],m[1, 1];int(1..2)]) = 2),
(m[0, 1] = m[1, 0])

--

Final model:

find m: matrix indexed by [int(0..1), int(0..1)] of int(0..1)

such that

(sum([SafeIndex(m,[0, 0]),SafeIndex(m,[1, 1]);int(1..)]) = 2),
(SafeIndex(m,[0, 1]) = SafeIndex(m,[1, 0]))

