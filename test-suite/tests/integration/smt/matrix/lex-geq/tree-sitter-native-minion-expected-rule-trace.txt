Model before rewriting:

find a: matrix indexed by [int(1..2)] of int(1..2)
find b: matrix indexed by [int(1..2)] of int(1..2)

such that

(a >=lex b),
a[1] = 1

--

(a >=lex b)
   ~~> normalise_lex_gt_geq ([("Base", 9000)])
(b <=lex a)

--

(b <=lex a),
a[1] = 1
   ~~> select_repr_mta ([("ReprMatrixToAtom", 8500)])
(b <=lex a),
a[1] = 1
new variables:
  find a#MatrixToAtom_1: int(1..2)
  find a#MatrixToAtom_2: int(1..2)
  find b#MatrixToAtom_1: int(1..2)
  find b#MatrixToAtom_2: int(1..2)

--

a[1]
   ~~> unsafe_const_index_matrix_to_atom ([("ReprMatrixToAtom", 6500)])
a#MatrixToAtom_1

--

(b <=lex a)
   ~~> matrix_ref_to_atom ([("ReprMatrixToAtom", 2000)])
([b#MatrixToAtom_1,b#MatrixToAtom_2;int(1..2)] <=lex [a#MatrixToAtom_1,a#MatrixToAtom_2;int(1..2)])

--

([b#MatrixToAtom_1,b#MatrixToAtom_2;int(1..2)] <=lex [a#MatrixToAtom_1,a#MatrixToAtom_2;int(1..2)])
   ~~> flatten_lex_lt_leq ([("Minion", 2000)])
FlatLexLeq([b#MatrixToAtom_1, b#MatrixToAtom_2], [a#MatrixToAtom_1, a#MatrixToAtom_2])

--

Final model:

find a: matrix indexed by [int(1..2)] of int(1..2)
find b: matrix indexed by [int(1..2)] of int(1..2)
find a#MatrixToAtom_1: int(1..2)
find a#MatrixToAtom_2: int(1..2)
find b#MatrixToAtom_1: int(1..2)
find b#MatrixToAtom_2: int(1..2)

such that

FlatLexLeq([b#MatrixToAtom_1, b#MatrixToAtom_2], [a#MatrixToAtom_1, a#MatrixToAtom_2]),
a#MatrixToAtom_1 = 1

