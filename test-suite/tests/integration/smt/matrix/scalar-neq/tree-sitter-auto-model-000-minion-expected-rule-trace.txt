Model before rewriting:

find m: matrix indexed by [int(1)] of bool
find m2: matrix indexed by [int(1)] of bool

such that

m != m2

--

m != m2
   ~~> select_matrix_components ([("ReprMatrixComponents", 8500)])
m != m2
new variables:
  find m#components_1: bool
  find m2#components_1: bool

--

m != m2
   ~~> flatten_matrix_eq_neq ([("Base", 3000)])
or([m[1] != m2[1];int(1..)])

--

or([m[1] != m2[1];int(1..)])
   ~~> remove_unit_vector_or ([("Base", 8800)])
m[1] != m2[1]

--

m[1]
   ~~> unsafe_const_index_matrix_components ([("ReprMatrixComponents", 6500)])
m#components_1

--

m2[1]
   ~~> unsafe_const_index_matrix_components ([("ReprMatrixComponents", 6500)])
m2#components_1

--

Final model:

find m: matrix indexed by [int(1)] of bool
find m2: matrix indexed by [int(1)] of bool
find m#components_1: bool
find m2#components_1: bool

such that

m#components_1 != m2#components_1

