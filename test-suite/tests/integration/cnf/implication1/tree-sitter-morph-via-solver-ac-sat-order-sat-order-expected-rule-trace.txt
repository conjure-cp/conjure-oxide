Model before rewriting:

find a: bool
find b: bool

such that

(a) -> (b)

--

Final model:

find a: bool
find b: bool
find __0: bool

such that

true

clauses:

(¬__0 \/ ¬a \/ b),
(__0 \/ a),
(__0 \/ ¬b),
(__0)

