# Imported from Conjure tests/exhaustive/basic/relation04_find/relation04.essence.
# Adapted: `forAll (i,j,k) in r . ...` (tuple-pattern comprehension generator) rewritten to
# `forAll i in r . i[1]*i[2]=i[3]` (single-var + tuple indexing) -- Oxide's native grammar does
# not yet support tuple-pattern generators for any collection type; this is a general gap, not
# specific to relations. Semantics are identical.
# Artefacts recorded with TEST_CASE_TIMEOUT=120.
parser = "tree-sitter"
rewriter = "optimised"
comprehension-expander = "native"
heuristic = "x"
solver = "minion"
number-of-solutions = "all"
