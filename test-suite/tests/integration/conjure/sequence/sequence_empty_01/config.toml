# Imported from Conjure tests/exhaustive/basic/sequence_empty_01 (first of three commented
# variants in seq.essence: a constant-only model).
# Artefacts recorded with TEST_CASE_TIMEOUT=120.
parser = "tree-sitter"
rewriter = "optimised"
comprehension-expander = "native"
heuristic = "x"
solver = "minion"
number-of-solutions = "all"
