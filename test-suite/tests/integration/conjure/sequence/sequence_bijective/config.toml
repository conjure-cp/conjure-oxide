# Hand-authored campaign verification case (not imported from Conjure's exhaustive suite):
# bijective sequence, exercising the combined allDiff + total-inverse-lookup constraints.
# Expected: 3! = 6 (every permutation of a 3-element domain is a bijection).
parser = "tree-sitter"
rewriter = "optimised"
comprehension-expander = "native"
heuristic = "x"
solver = "minion"
number-of-solutions = "all"
