# Imported from Conjure tests/exhaustive/basic/sequence_subseq_nodups.
# Expected: (), 3, 1, 2, 31, 32, 12 (six non-empty subsequences plus the empty one).
# Artefacts recorded with TEST_CASE_TIMEOUT=120.
parser = "tree-sitter"
rewriter = "optimised"
comprehension-expander = "native"
heuristic = "x"
solver = "minion"
number-of-solutions = "all"
