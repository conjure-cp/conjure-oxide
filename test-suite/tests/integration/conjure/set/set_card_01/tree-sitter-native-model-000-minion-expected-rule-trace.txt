Model before rewriting:

find s: set (size 1) of int(1..2)

such that

|s| in s

--

s
   ~~> select_representation ([("ReprGeneral", 10000)])
s
new variables:
  find s#explicit_1: matrix indexed by [int(1..1)] of int(1..2)
new constraints:
  or([1 <= 1,SafeIndex(s#explicit_1,[1]) = 1;int(1..)])

--

s
   ~~> select_representation ([("ReprGeneral", 10000)])
s

--

|s| in s
   ~~> in_explicit ([("ReprGeneral", 9500)])
or([and([1 <= 1,SafeIndex(s#explicit_1,[1]) = |s|;int(1..)]);int(1..)])

--

|s|
   ~~> cardinality_explicit ([("ReprGeneral", 9500)])
1

--

or([and([SafeIndex(s#explicit_1,[1]) = 1;int(1..)]);int(1..)])
   ~~> remove_unit_vector_or ([("Base", 8800)])
and([SafeIndex(s#explicit_1,[1]) = 1;int(1..)])

--

and([SafeIndex(s#explicit_1,[1]) = 1;int(1..)])
   ~~> remove_unit_vector_and ([("Base", 8800)])
SafeIndex(s#explicit_1,[1]) = 1

--

SafeIndex(s#explicit_1,[1]) = 1,
true
   ~~> select_matrix_components ([("ReprMatrixComponents", 8500)])
SafeIndex(s#explicit_1,[1]) = 1,
true
new variables:
  find s#explicit_1#components_1: int(1..2)

--

SafeIndex(s#explicit_1,[1])
   ~~> index_matrix_components ([("ReprMatrixComponents", 5000)])
s#explicit_1#components_1

--

s#explicit_1#components_1 = 1
   ~~> eq_atom_literal_to_wliteral ([("Minion", 4400)])
WatchedLiteral(s#explicit_1#components_1,1)

--

Final model:

find s: set{explicit} (size 1) of int(1..2)
find s#explicit_1: matrix indexed by [int(1..1)] of int(1..2)
find s#explicit_1#components_1: int(1..2)

such that

WatchedLiteral(s#explicit_1#components_1,1),
true

