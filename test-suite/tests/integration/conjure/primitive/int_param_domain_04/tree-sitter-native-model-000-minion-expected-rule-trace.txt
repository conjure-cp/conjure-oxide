Model before rewriting:

letting s be {1,2,3,4,5}
find x: int((min(s))..(max(s)))

such that

x in s

--

x in {1,2,3,4,5}
   ~~> in_set ([("Minion", 1)])
__minion_w_inset(x,[1,2,3,4,5])

--

Final model:

letting s be {1,2,3,4,5}
find x: int((min(s))..(max(s)))

such that

__minion_w_inset(x,[1,2,3,4,5])

