Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

x = partition({-1}) = partition({-2}),
y = partition({1}) = partition({2}),
z = partition({1}) = partition({1})

--

Final model:

find x: bool
find y: bool
find z: bool

such that

x = false,
y = false,
z

