Model before rewriting:

find x: bool
find y: bool
find z: bool

such that

or([or([x,y;int(1..)]),z;int(1..)])

--

or([or([x,y;int(1..)]),z;int(1..)]), 
   ~~> normalise_associative_commutative ([("Base", 8900)])
or([x,y,z;int(1..)])

--

Final model:

find x: bool
find y: bool
find z: bool

such that

or([x,y,z;int(1..)])

