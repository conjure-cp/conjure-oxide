parser = "tree-sitter"
rewriter = "optimised"
comprehension-expander = "auto"
solver = "minion"
# Enumerating every solution is not the point here and would take far longer than finding the
# one the puzzle has.
number-of-solutions = 1
