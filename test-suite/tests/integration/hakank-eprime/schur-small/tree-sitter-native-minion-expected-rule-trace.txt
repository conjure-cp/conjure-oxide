Model before rewriting:

letting n be 2
letting num_boxes be 3
find schur: matrix indexed by [int(1..n)] of int(1..num_boxes)

such that

and([ (sum([i,j;int(1..)]) = k) -> (!(and([schur[i] = schur[j],schur[j] = schur[k];int(1..)]))) | i : int(1..n), j : int(1..n), k : int(1..n) ])

--

and([ (sum([i,j;int(1..)]) = k) -> (!(and([schur[i] = schur[j],schur[j] = schur[k];int(1..)]))) | i : int(1..n), j : int(1..n), k : int(1..n) ])
   ~~> select_repr_mta ([("ReprMatrixToAtom", 8500)])
and([ (sum([i,j;int(1..)]) = k) -> (!(and([schur[i] = schur[j],schur[j] = schur[k];int(1..)]))) | i : int(1..n), j : int(1..n), k : int(1..n) ])
new variables:
  find schur#MatrixToAtom_1: int(1..3)
  find schur#MatrixToAtom_2: int(1..3)

--

[ (sum([i,j;int(1..)]) = k) -> (!(and([schur[i] = schur[j],schur[j] = schur[k];int(1..)]))) | i : int(1..n), j : int(1..n), k : int(1..n) ]
   ~~> expand_comprehension_native ([("Base", 2000)])
[!(and([schur#MatrixToAtom_1 = schur#MatrixToAtom_2;int(1..)]));int(1..)]

--

and([!(and([schur#MatrixToAtom_1 = schur#MatrixToAtom_2;int(1..)]));int(1..)])
   ~~> remove_unit_vector_and ([("Base", 8800)])
!(and([schur#MatrixToAtom_1 = schur#MatrixToAtom_2;int(1..)]))

--

and([schur#MatrixToAtom_1 = schur#MatrixToAtom_2;int(1..)])
   ~~> remove_unit_vector_and ([("Base", 8800)])
schur#MatrixToAtom_1 = schur#MatrixToAtom_2

--

!(schur#MatrixToAtom_1 = schur#MatrixToAtom_2)
   ~~> negated_eq_to_neq ([("Base", 8800)])
schur#MatrixToAtom_1 != schur#MatrixToAtom_2

--

Final model:

letting n be 2
letting num_boxes be 3
find schur: matrix indexed by [int(1..n)] of int(1..num_boxes)
find schur#MatrixToAtom_1: int(1..3)
find schur#MatrixToAtom_2: int(1..3)

such that

schur#MatrixToAtom_1 != schur#MatrixToAtom_2

