Model before rewriting:

letting a be {2,3}
letting b be {1,2}
find c: set (size 1) of int(1..3)

such that

c = (a intersect b)

--

c
   ~~> select_representation ([("ReprGeneral", 10000)])
c
new variables:
  find c#explicit_1: matrix indexed by [int(1..1)] of int(1..3)
new constraints:
  or([1 <= 1,SafeIndex(c#explicit_1,[1]) = 1;int(1..)])

--

c = {2},
true
   ~~> select_representation_unconstrained ([("ReprGeneral", 9900)])
c = {2},
true
new variables:
  find c#explicit_1#components_1: int(1..3)

--

c = {2}
   ~~> eq_explicit_literal ([("ReprGeneral", 9500)])
and([1 = 1,or([and([1 <= 1,SafeIndex(c#explicit_1,[1]) = 2;int(1..)]);int(1..)]);int(1..)])

--

c#explicit_1
   ~~> select_representation ([("ReprGeneral", 10000)])
c#explicit_1

--

and([or([and([SafeIndex(c#explicit_1,[1]) = 2;int(1..)]);int(1..)]);int(1..)])
   ~~> remove_unit_vector_and ([("Base", 8800)])
or([and([SafeIndex(c#explicit_1,[1]) = 2;int(1..)]);int(1..)])

--

or([and([SafeIndex(c#explicit_1,[1]) = 2;int(1..)]);int(1..)])
   ~~> remove_unit_vector_or ([("Base", 8800)])
and([SafeIndex(c#explicit_1,[1]) = 2;int(1..)])

--

and([SafeIndex(c#explicit_1,[1]) = 2;int(1..)])
   ~~> remove_unit_vector_and ([("Base", 8800)])
SafeIndex(c#explicit_1,[1]) = 2

--

SafeIndex(c#explicit_1,[1])
   ~~> index_matrix_components ([("ReprMatrixComponents", 5000)])
c#explicit_1#components_1

--

c#explicit_1#components_1 = 2
   ~~> eq_atom_literal_to_wliteral ([("Minion", 4400)])
WatchedLiteral(c#explicit_1#components_1,2)

--

Final model:

letting a be {2,3}
letting b be {1,2}
find c: set (representation explicit, size 1) of int(1..3)
find c#explicit_1: matrix indexed by [int(1..1)] of int(1..3)
find c#explicit_1#components_1: int(1..3)

such that

WatchedLiteral(c#explicit_1#components_1,2),
true

