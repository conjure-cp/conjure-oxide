Model before rewriting:

find x: bool
find y: bool

such that

and([(x) <-> (x),(false) <-> (y);int(1..)])

--

and([!(y);int(1..)])
   ~~> remove_unit_vector_and ([("Base", 8800)])
!(y)

--

Final model:

find x: bool
find y: bool

such that

!(y)

