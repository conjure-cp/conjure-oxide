Model before rewriting:

find y: bool

such that

y = and([ product([j,j;int(1..)]) >= j | j : int(0..3) ])

--

y
   ~~> remove_single_atom ([("SAT", 8400)])
true
new clauses:
  (y)

--

Final model:

find y: bool

such that

true

clauses:

(y)

