Model before rewriting:

find result: bool

such that

result = and([ and([n > 0,n < 10;int(1..)]) | n : int(1..3) ])

--

result
   ~~> remove_single_atom ([("SAT", 8400)])
true
new clauses:
  (result)

--

Final model:

find result: bool

such that

true

clauses:

(result)

