Model before rewriting:

find result: bool

such that

and([(result = and([ (i > 0) | i : int(1..3) ])),or([ (j = 2) | j <- [1,2,3;int(1..3)] ]);int(1..)])

--

and([(result = and([ (i > 0) | i : int(1..3) ])),or([ (j = 2) | j <- [1,2,3;int(1..3)] ]);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
(result = true),
or([ (j = 2) | j <- [1,2,3;int(1..3)] ])

--

(result = true), 
   ~~> bool_eq_to_reify ([("Minion", 4400)])
Reify(true, result)

--

Reify(true, result),
or([ (j = 2) | j <- [1,2,3;int(1..3)] ]), 
   ~~> exists_quantified_to_finds ([("Base", 2003)])
Reify(true, result),
(__0 = 2)
new variables:
  find __0: int(1..3)

--

Final model:

find result: bool
find __0: int(1..3)

such that

Reify(true, result),
(__0 = 2)

