Model before rewriting:

find x: bool

such that

(x = allDiff([ x | x : int(1..10), (x % 2 = 0) ]))

--

(x = allDiff([ x | x : int(1..10), (x % 2 = 0) ])), 
   ~~> constant_evaluator ([("Constant", 9001)])
(x = true)

--

Final model:

find x: bool

such that

(x = true)

