Model before rewriting:

find x: int(0..1000)

such that

x = sum([ j | i : set (minSize 1, maxSize 2) of int(7..8), j <- i ])

--

x = 30
   ~~> eq_atom_literal_to_wliteral ([("Minion", 4400)])
WatchedLiteral(x,30)

--

Final model:

find x: int(0..1000)

such that

WatchedLiteral(x,30)

