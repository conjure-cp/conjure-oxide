parser = "tree-sitter"
rewriter = "naive"
comprehension-expander = "native"
solver = "minion"
number-of-solutions = 1
skip-conjure-validation = "Conjure rejects decision variables in domain bounds (e.g. int(1..x)) [confirmed]"
expected-time = 5
status = "ok"

[stats]

[stats.oxide]
status = "ok"
