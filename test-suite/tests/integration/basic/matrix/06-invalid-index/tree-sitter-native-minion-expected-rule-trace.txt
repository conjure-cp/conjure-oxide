Model before rewriting:

letting MATRIX be domain matrix indexed by [int(1..3), int(1..2)] of int(1..3)
find a: MATRIX

such that

allDifferent(a[..,1]),
allDifferent(a[..,2]),
allDifferent(a[1,..]),
allDifferent(a[2,..]),
allDifferent(a[3,..]),
a[1, 1] = 1,
a[2, 3] = 1

--

allDifferent(a#matrix_to_atom[..,1]),
allDifferent(a#matrix_to_atom[..,2]),
allDifferent(a#matrix_to_atom[1,..]),
allDifferent(a#matrix_to_atom[2,..]),
allDifferent(a#matrix_to_atom[3,..]),
a#matrix_to_atom[1, 1] = 1,
a#matrix_to_atom[2, 3] = 1
   ~~> select_representation_matrix ([("Representations", 8001)])
allDifferent(a#matrix_to_atom[..,1]),
allDifferent(a#matrix_to_atom[..,2]),
allDifferent(a#matrix_to_atom[1,..]),
allDifferent(a#matrix_to_atom[2,..]),
allDifferent(a#matrix_to_atom[3,..]),
a#matrix_to_atom[1, 1] = 1,
a#matrix_to_atom[2, 3] = 1
new variables:
  find a#matrix_to_atom_1_1: int(1..3)
  find a#matrix_to_atom_1_2: int(1..3)
  find a#matrix_to_atom_2_1: int(1..3)
  find a#matrix_to_atom_2_2: int(1..3)
  find a#matrix_to_atom_3_1: int(1..3)
  find a#matrix_to_atom_3_2: int(1..3)

--

a#matrix_to_atom[..,1]
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a#matrix_to_atom,[..,1])

--

a#matrix_to_atom[..,2]
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a#matrix_to_atom,[..,2])

--

a#matrix_to_atom[1,..]
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a#matrix_to_atom,[1,..])

--

a#matrix_to_atom[2,..]
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a#matrix_to_atom,[2,..])

--

a#matrix_to_atom[3,..]
   ~~> slice_to_bubble ([("Bubble", 6000)])
SafeSlice(a#matrix_to_atom,[3,..])

--

a#matrix_to_atom[1, 1]
   ~~> index_to_bubble ([("Bubble", 6000)])
SafeIndex(a#matrix_to_atom,[1, 1])

--

a#matrix_to_atom[2, 3]
   ~~> index_to_bubble ([("Bubble", 6000)])
{SafeIndex(a#matrix_to_atom,[2, 3]) @ false}

--

{SafeIndex(a#matrix_to_atom,[2, 3]) @ false} = 1
   ~~> bubble_up ([("Bubble", 8800)])
{SafeIndex(a#matrix_to_atom,[2, 3]) = 1 @ false}

--

{SafeIndex(a#matrix_to_atom,[2, 3]) = 1 @ false}
   ~~> expand_bubble ([("Bubble", 8900)])
and([SafeIndex(a#matrix_to_atom,[2, 3]) = 1,false;int(1..)])

--

allDifferent(SafeSlice(a#matrix_to_atom,[..,1])),
allDifferent(SafeSlice(a#matrix_to_atom,[..,2])),
allDifferent(SafeSlice(a#matrix_to_atom,[1,..])),
allDifferent(SafeSlice(a#matrix_to_atom,[2,..])),
allDifferent(SafeSlice(a#matrix_to_atom,[3,..])),
SafeIndex(a#matrix_to_atom,[1, 1]) = 1,
and([SafeIndex(a#matrix_to_atom,[2, 3]) = 1,false;int(1..)])
   ~~> constant_evaluator ([("Constant", 9001)])
false

--

Final model:

letting MATRIX be domain matrix indexed by [int(1..3), int(1..2)] of int(1..3)
find a: MATRIX
find a#matrix_to_atom_1_1: int(1..3)
find a#matrix_to_atom_1_2: int(1..3)
find a#matrix_to_atom_2_1: int(1..3)
find a#matrix_to_atom_2_2: int(1..3)
find a#matrix_to_atom_3_1: int(1..3)
find a#matrix_to_atom_3_2: int(1..3)

such that

false

