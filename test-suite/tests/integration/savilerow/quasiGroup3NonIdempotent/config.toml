parser = "tree-sitter"
rewriter = "optimised"
comprehension-expander = "native"
solver = ["ortools-cpsat"]
number-of-solutions = 1
skip-conjure-validation = "SavileRow FlatZinc translator shifts 0-indexed domains to 1-indexed domains and ignores solution limits"
