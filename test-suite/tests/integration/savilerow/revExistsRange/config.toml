parser = "tree-sitter"
rewriter = "naive"
comprehension-expander = "native"
solver = "minion"
number-of-solutions = 1
skip-conjure-validation = "Model typo: undefined reference m (should be M)"
status = "fail"

[stats]

[stats.oxide]
status = "fail"
