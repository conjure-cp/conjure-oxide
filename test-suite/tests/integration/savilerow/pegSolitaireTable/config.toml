parser = "tree-sitter"
rewriter = "naive"
comprehension-expander = "native"
solver = "minion"
number-of-solutions = 1
skip-conjure-validation = "Conjure rejects bool expressions in table constraints"
status = "fail"

[stats]

[stats.oxide]
status = "fail"
