Model before rewriting:

letting n be 3
find m: matrix indexed by [int(4..n)] of bool

such that

true

--

true
   ~~> select_representation_unconstrained ([("ReprGeneral", 9900)])
true
new variables:
  find m#array_1: matrix indexed by [int()] of bool

--

Final model:

letting n be 3
find m: matrix indexed by [int(4..n)] of bool
find m#array_1: matrix indexed by [int()] of bool

such that

true

