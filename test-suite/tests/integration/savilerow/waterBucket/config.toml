parser = "tree-sitter"
rewriter = "naive"
comprehension-expander = "native"
solver = "minion"
number-of-solutions = 1
skip-conjure-validation = "Conjure type error: min expects a domain argument, not an int expression"
status = "fail"

[stats]

[stats.oxide]
status = "fail"
