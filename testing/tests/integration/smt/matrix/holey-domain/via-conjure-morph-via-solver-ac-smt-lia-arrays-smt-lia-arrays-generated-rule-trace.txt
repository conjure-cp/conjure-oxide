Model before rewriting:

find a: matrix indexed by [int(1..2, 4)] of bool

such that

and([a[1],a[2],a[4];int(1..3)])

--

Final model:

find a: matrix indexed by [int(1..2, 4)] of bool

such that

SafeIndex(a,[1]),
SafeIndex(a,[2]),
SafeIndex(a,[4])

