Model before rewriting:

find m: matrix indexed by [int(1..2)] of bool
find x: int(1..3)

such that

m[x]

--

m[x], 
   ~~> index_to_bubble ([("Bubble", 6000)])
{SafeIndex(m,[x]) @ __inDomain(x,int(1..2))}

--

{SafeIndex(m,[x]) @ __inDomain(x,int(1..2))}, 
   ~~> expand_bubble ([("Bubble", 8900)])
and([SafeIndex(m,[x]),__inDomain(x,int(1..2));int(1..)])

--

and([SafeIndex(m,[x]),__inDomain(x,int(1..2));int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
SafeIndex(m,[x]),
__inDomain(x,int(1..2))

--

__inDomain(x,int(1..2)), 
   ~~> flatten_indomain ([("Smt", 1000)])
or([and([(x >= 1),(x <= 2);int(1..)]);int(1..)])

--

or([and([(x >= 1),(x <= 2);int(1..)]);int(1..)]), 
   ~~> remove_unit_vector_or ([("Base", 8800)])
and([(x >= 1),(x <= 2);int(1..)])

--

SafeIndex(m,[x]),
and([(x >= 1),(x <= 2);int(1..)]), 
   ~~> constant_evaluator ([("Constant", 9001)])
SafeIndex(m,[x]),
(x >= 1),
(x <= 2)

--

Final model:

find m: matrix indexed by [int(1..2)] of bool
find x: int(1..3)

such that

SafeIndex(m,[x]),
(x >= 1),
(x <= 2)

