Model before rewriting:

find a: bool
find b: bool
find c: bool
find d: bool

such that

and([or([a,a;int(1..2)]),or([Not(b),Not(b);int(1..2)]);int(1..2)])

--

and([or([a,a;int(1..2)]),or([Not(b),Not(b);int(1..2)]);int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)]) 
and([or([a,a;int(1..2)]),or([Not(b),Not(b);int(1..2)]);int(1..)]) 

--

and([or([a,a;int(1..2)]),or([Not(b),Not(b);int(1..2)]);int(1..)]), 
   ~~> partial_evaluator ([("Base", 9000)]) 
or([a,a;int(1..2)]),
or([Not(b),Not(b);int(1..2)]) 

--

or([a,a;int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)]) 
or([a,a;int(1..)]) 

--

or([Not(b),Not(b);int(1..2)]), 
   ~~> matrix_to_list ([("Base", 2000)]) 
or([Not(b),Not(b);int(1..)]) 

--

Final model:

find a: bool
find b: bool
find c: bool
find d: bool

such that

or([a,a;int(1..)]),
or([Not(b),Not(b);int(1..)])

